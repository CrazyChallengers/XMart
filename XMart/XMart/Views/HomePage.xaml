<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:ff="clr-namespace:FFImageLoading.Forms;assembly=FFImageLoading.Forms"
             xmlns:controls="clr-namespace:XMart.Controls"
             xmlns:cv="clr-namespace:CarouselView.FormsPlugin.Abstractions;assembly=CarouselView.FormsPlugin.Abstractions"
             x:Class="XMart.Views.HomePage"
             Icon="Resource/drawable/home.png"
             Title="首页">


    <ContentPage.Content>
        <Grid x:Name="MainGrid" RowSpacing="0" BackgroundColor="Transparent">
            <Grid.RowDefinitions>
                <RowDefinition Height="100"/>
                <RowDefinition/>
            </Grid.RowDefinitions>

            <Image Grid.Row="0" Grid.RowSpan="2" Source="Resource/drawable/navi_back.png" VerticalOptions="StartAndExpand" HorizontalOptions="Fill" Aspect="AspectFit"/>

            <!--头部-->
            <StackLayout Grid.Row="0" Orientation="Horizontal" HorizontalOptions="Fill" VerticalOptions="CenterAndExpand">
                <ImageButton x:Name="CustomerServiceButton" Source="Resource/drawable/service.png" Style="{StaticResource EarImageButton}"/>

                <Frame Style="{DynamicResource SearchFrame}">
                    <controls:MyEntry x:Name="account" Placeholder="原料更便宜啦~质量更好、价格低" Text="{Binding Account}" Style="{StaticResource SearchEntry}"/>
                </Frame>

                <ImageButton x:Name="MessageButton" Source="Resource/drawable/message.png" Style="{StaticResource EarImageButton}" Clicked="MessageButton_Clicked"/>
            </StackLayout>

            <ScrollView Grid.Row="1">
                <StackLayout Orientation="Vertical" HorizontalOptions="Center">
                    <!--轮播图-->
                    <Frame Style="{StaticResource CarouselFrame}">
                        <cv:CarouselViewControl x:Name="carousel" ItemsSource="{Binding AdvertiseList}"
                                        ShowArrows="False" ShowIndicators="True" Orientation="Horizontal">
                            <cv:CarouselViewControl.ItemTemplate>
                                <DataTemplate>
                                    <ImageButton Source="{Binding pic}"
                                         Command="{Binding CarouselSelectedCommand}"
                                         CommandParameter="{Binding url}"
                                         BackgroundColor="Transparent" Aspect="AspectFill"/>
                                </DataTemplate>
                            </cv:CarouselViewControl.ItemTemplate>
                        </cv:CarouselViewControl>
                    </Frame>

                    <!--主功能矩阵-->
                    <Frame Style="{StaticResource MenuFrame}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="50"/>
                                <RowDefinition Height="20"/>
                                <RowDefinition Height="50"/>
                                <RowDefinition Height="20"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <Grid.Resources>
                                <Style TargetType="ImageButton">
                                    <Setter Property="WidthRequest" Value="50"/>
                                    <Setter Property="HeightRequest" Value="50"/>
                                    <Setter Property="BackgroundColor" Value="Transparent"/>
                                    <!--<Setter Property="VisualStateManager.VisualStateGroups">
                <VisualStateGroupList>
                    <VisualStateGroup x:Name="CommonStates">
                        <VisualState x:Name="Normal">
                            <VisualState.Setters>
                                <Setter Property="Scale" Value="1" />
                            </VisualState.Setters>
                        </VisualState>

                        <VisualState x:Name="Pressed">
                            <VisualState.Setters>
                                <Setter Property="Scale" Value="0.9" />
                            </VisualState.Setters>
                        </VisualState>
                    </VisualStateGroup>
                </VisualStateGroupList>
            </Setter>-->
                                </Style>
                                <Style TargetType="Label">
                                    <Setter Property="HorizontalOptions" Value="CenterAndExpand"/>
                                    <Setter Property="VerticalOptions" Value="CenterAndExpand"/>
                                    <Setter Property="HorizontalTextAlignment" Value="Center"/>
                                    <Setter Property="VerticalTextAlignment" Value="Center"/>
                                    <Setter Property="FontSize" Value="Medium"/>
                                </Style>
                            </Grid.Resources>

                            <ImageButton Grid.Row="0" Grid.Column="0" Source="Resource/drawable/circle.png" />
                            <Label Grid.Row="1" Grid.Column="0" Text="房源管理"/>

                            <ImageButton Grid.Row="0" Grid.Column="1" Source="Resource/drawable/circle.png"/>
                            <Label Grid.Row="1" Grid.Column="1" Text="客源管理"/>

                            <ImageButton Grid.Row="0" Grid.Column="2" Source="Resource/drawable/circle.png" />
                            <Label Grid.Row="1" Grid.Column="2" Text="外网房源"/>

                            <ImageButton Grid.Row="0" Grid.Column="3" Source="Resource/drawable/circle.png"/>
                            <Label Grid.Row="1" Grid.Column="3" Text="新闻公告"/>

                            <ImageButton Grid.Row="0" Grid.Column="4" Source="Resource/drawable/circle.png" />
                            <Label Grid.Row="1" Grid.Column="4" Text="工作总结"/>

                            <ImageButton Grid.Row="0" Grid.Column="5" Source="Resource/drawable/circle.png" />
                            <Label Grid.Row="1" Grid.Column="5" Text="业务报表"/>

                            <ImageButton Grid.Row="2" Grid.Column="0" Source="Resource/drawable/circle.png" />
                            <Label Grid.Row="3" Grid.Column="0" Text="跟进记录"/>

                            <ImageButton Grid.Row="2" Grid.Column="1" Source="Resource/drawable/circle.png"/>
                            <Label Grid.Row="3" Grid.Column="1" Text="通讯录"/>

                            <ImageButton Grid.Row="2" Grid.Column="2" Source="Resource/drawable/circle.png"/>
                            <Label Grid.Row="3" Grid.Column="2" Text="房贷计算"/>

                            <ImageButton Grid.Row="2" Grid.Column="3" Source="Resource/drawable/circle.png"/>
                            <Label Grid.Row="3" Grid.Column="3" Text="税费计算"/>

                            <ImageButton Grid.Row="2" Grid.Column="4" Source="Resource/drawable/circle.png"/>
                            <Label Grid.Row="3" Grid.Column="4" Text="新增房源"/>

                            <ImageButton Grid.Row="2" Grid.Column="5" Source="Resource/drawable/circle.png"/>
                            <Label Grid.Row="3" Grid.Column="5" Text="新增客源"/>

                        </Grid>
                    </Frame>
                    
                    <!--折扣商品列表-->
                    <Label Text="热卖好货" FontSize="Title" HorizontalOptions="Start" Margin="5,0"/>
                    <ScrollView Orientation="Horizontal" HorizontalScrollBarVisibility="Never">
                        <StackLayout Orientation="Horizontal" BindableLayout.ItemsSource="{Binding HotProductList}">
                            <BindableLayout.ItemTemplate>
                                <DataTemplate>
                                    <Frame HeightRequest="180" WidthRequest="120" BorderColor="LightGray" HasShadow="True" 
                                           CornerRadius="5" Margin="5" Padding="0" IsClippedToBounds="True">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="120"/>
                                                <RowDefinition/>
                                                <RowDefinition/>
                                            </Grid.RowDefinitions>

                                            <Image Grid.Row="0" Source="{Binding pic}" Aspect="AspectFill"/>
                                            <Label Grid.Row="1" Text="{Binding name}" FontSize="Large"/>
                                            <Label Grid.Row="2" TextColor="Red">
                                                <Label.FormattedText>
                                                    <FormattedString>
                                                        <Span Text="￥"/>
                                                        <Span Text="{Binding price}"/>
                                                    </FormattedString>
                                                </Label.FormattedText>
                                            </Label>
                                        </Grid>
                                    </Frame>
                                </DataTemplate>
                            </BindableLayout.ItemTemplate>
                        </StackLayout>
                    </ScrollView>

                    <!--专题列表-->
                    <Label Text="精彩专题" FontSize="Title" HorizontalOptions="Start" Margin="5,0"/>
                    <ScrollView Orientation="Horizontal" HorizontalScrollBarVisibility="Never">
                        <StackLayout Orientation="Horizontal" BindableLayout.ItemsSource="{Binding SubjectList}">
                            <BindableLayout.ItemTemplate>
                                <DataTemplate>
                                    <Frame HeightRequest="250" WidthRequest="200" BorderColor="LightGray" HasShadow="True" 
                                           CornerRadius="5" Margin="5" Padding="0" IsClippedToBounds="True">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="200"/>
                                                <RowDefinition/>
                                            </Grid.RowDefinitions>

                                            <Image Grid.Row="0" Grid.RowSpan="2" Source="{Binding pic}" Aspect="AspectFill"/>
                                            <Label Grid.Row="1" Text="{Binding title}" FontSize="Large"/>
                                        </Grid>
                                    </Frame>
                                </DataTemplate>
                            </BindableLayout.ItemTemplate>
                        </StackLayout>
                    </ScrollView>

                </StackLayout>
            </ScrollView>

        </Grid>
    </ContentPage.Content>
</ContentPage>